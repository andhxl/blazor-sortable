@page "/"
@using BlazorSortableDemo.Pages.Examples
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(
            ExampleCatalog.GetRelativePath(ExampleCatalog.All[0].Id),
            replace: true);
    }
}
