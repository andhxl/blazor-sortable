@page "/"

@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo(ExampleCatalog.GetPath(ExampleCatalog.All[0].Id), replace: true);
    }
}
